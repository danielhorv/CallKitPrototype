# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

def shared_pods
    pod "RxSwift"
    pod "XCGLogger"
end

target 'CallKitPrototype' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod "RxCocoa"
  pod "RxDataSources"
  pod "RxOptional"
  pod "ReactorKit"
  pod "RxGesture"
  pod "RxKeyboard"
  pod "RxAnimated"
  pod "RxCoreLocation"
  pod "TAReusableView"

end

target 'PrototypeKit' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  shared_pods
end
